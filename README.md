# Riad Diamond — Site Next.js

Projet template créé avec Next.js (App Router), TypeScript, Tailwind CSS et Nodemailer.

Scripts:

```
npm install
npm run dev
```

Configurer `.env.local` en vous basant sur `.env.local.example`.
